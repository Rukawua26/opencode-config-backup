# Roadmap

## Candidate Areas

- Refine SDD skills after first real project usage.
- Add command documentation examples for `/spec`, `/plan-spec`, `/tasks-spec`, `/implement-spec`, and `/verify-spec`.
- Review skills for overlap and token cost.
- Add lightweight validation for skill file structure if needed.

Detailed workflow changes should be specified under `spec/features/NNN-name/`.
